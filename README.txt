The file logisticRegression.py uses the numpy and matplotlib packages.
The main function call contains the file names as input1 and input2. The learning rate, alpha is set to 0.001 which is defined in the LogisticRegression class as alpha. The number of iterations are passed as a paramter when the model is created in the main function call. Here the value given is 1000.

On mentioning the above parameters, the files can be run normally.